# BLE OTA functionality
bleak>=0.21.0

# Grinder Data Analysis Tool Dependencies  
pyserial>=3.5
pandas>=2.0.0
matplotlib>=3.3.0
scipy>=1.9.0

# Streamlit visualization and reporting
streamlit>=1.28.0
plotly>=5.15.0

# Build system (use PlatformIO from the venv, not global `pio`)
# PlatformIO 6.1.19+ requires Python >= 3.10; create tools/venv with python3.10+.
platformio>=6.1.19

detools>=0.53.0

# Required by the espressif32 platform build (component_manager)
pyyaml>=6.0

# Boot logo conversion (tools/convert_logo.py + LVGL's LVGLImage.py)
pillow>=10.0.0
pypng>=0.0.20
lz4>=4.0.0

# Cross-platform color support
colorama>=0.4.0
